name: spectora_interview_code_flutter
description: Spectora interview code challenge.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '^3.1.0'

dependencies:
  flutter:
    sdk: flutter

  hooks_riverpod: ^2.4.0
  flutter_hooks: ^0.20.1
  freezed_annotation: ^2.2.0
  json_annotation: ^4.8.1
  cupertino_icons: ^1.0.2
  positive_num: ^1.0.8+1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  freezed: ^2.3.5
  json_serializable: ^6.6.2
  flutter_gen: ^5.3.1
  mocktail: ^1.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/data/